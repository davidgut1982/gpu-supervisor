# gpu-supervisor dependencies
#
# CPU-only service: no torch, no GPU packages.
# All dependencies are small and available on PyPI.

fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.0
pydantic-settings>=2.0
